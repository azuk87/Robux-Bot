generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

model Users {
  id                      String  @id @map("_azuk8709")
  guildId                 String
  balance                 Int     @default(9999)
  blacklist               Boolean @default(false)
  buyTotal                Int     @default(10000)
  buyedCount              Int     @default(0)
  transactionsTotal       Int     @default(100p0)
  transactionsCount       Int     @default(100000)
  lastTransactionsAccount String?
}

model Guilds {
  id            String  @id @map("_azuk8709")
  prefix        String  @default("!")
  cookie        String?
  group         Int?
  recipient     String?
  price         Int     @default(1000)
  proofsChannel String?
  thxChannel    String?
  thxEmoji      String?
  codesChannel  String?
  logsChannel   String?
  clientsRole   String?
  buy           Status
  transfer      Status
}

type Status {
  min    Int     @default(1)
  max    Int     @default(1000)
  status Boolean @default(true)
}
